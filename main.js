/**
 * Your task is to write a function named `checkMonthLength` which takes in the name of a month and returns true if the month has 31 days and false otherwise.
 *
 * The function should handle both full month names and abbreviated forms (e.g., "January" and "Jan").
 *
 * Examples:
 *    checkMonthLength("January") should return true
 *    checkMonthLength("Feb") should return false
 */
export function checkMonthLength(month) {
  // Good luck!
}

/**
 * Your task is to write a function named `isLeapYear` which takes in a year as input and returns true if it is a leap year and false otherwise.
 *
 * Examples:
 *    isLeapYear(2000) should return true
 *    isLeapYear(1900) should return false
 *    isLeapYear(2024) should return true
 */
export function isLeapYear(year) {
  // Good luck!
}


  
  
  